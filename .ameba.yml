# Ameba configuration — https://github.com/crystal-ameba/ameba
#
# Run with: ./bin/ameba   (auto-fix: ./bin/ameba --fix)

# TODO/FIXME/etc. notes in comments are intentional roadmap markers here
# (e.g. the "Add TrueColor functionality" note), not actionable debt.
Documentation/DocumentationAdmonition:
  Enabled: false
